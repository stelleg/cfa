(\iszero.
  (\pred.
    (\sub.
      (\n.
        (iszero ((sub (n n)) (n n)))
      \f.\x.(f (f (f x))))
    \m.\n.((n pred) m))
  \n.\f.\x.(((n \g.\h.(h (g f))) \u.x) \u.u))
\n.((n \z.\x.\y.y) \x.\y.x))

