(\iszero.
  (\pred.
    (\sub.
      (\ten.
        (iszero ((sub ten) (pred ten)))
      \f.\x.(f (f (f (f (f (f (f (f (f (f x)))))))))))
    \m.\n.((n pred) m))
  \n.\f.\x.(((n \g.\h.(h (g f))) \u.x) \u.u))
\n.((n \x.\x.\y.y) \x.\y.x))
